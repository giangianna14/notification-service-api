# Core API framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Pydantic v2 + settings
pydantic==2.7.1
pydantic-settings==2.2.1
email-validator==2.1.1       # enables EmailStr in pydantic

# Environment variables
python-dotenv==1.0.1

# HTTP client (used by health check tests)
httpx==0.27.0

# WhatsApp via Twilio (optional — service mocks when not installed)
# twilio==9.0.5

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
